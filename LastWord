package main

import (
	"fmt"
)


func LastWord(s string) string {

	lastWord := ""
	end := len(s) - 1

	for end >= 0 && s[end] == ' ' {
		end--
	}

	for end >= 0 && s[end] != ' ' {
		lastWord = string(s[end]) + lastWord 
		end--
	}

	return lastWord
}

